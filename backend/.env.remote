# ============================================
# CONFIGURACIÓN SERVIDOR REMOTO - COMPLETA ESTOS DATOS
# ============================================
# Esta es la configuración para conectarte al servidor remoto a través de VPN

# Puerto del servidor (probablemente el mismo)
PORT=5000

# Configuración de Base de Datos SQL Server REMOTO
# IMPORTANTE: Completa estos datos con la información del servidor remoto
DB_HOST=dwin0293              # IP o nombre del servidor remoto (ej: 192.168.1.100 o servidor.empresa.com)
DB_PORT=1433          # Puerto por defecto de SQL Server (ajusta si es diferente)
DB_USER=calidad # Usuario de SQL Server en el servidor remoto
DB_PASSWORD=passcalidad # Contraseña del usuario remoto
#DB_NAME=taskmanagementsystem  # Nombre de la base de datos (debería ser el mismo)

# JWT Secret (mantener el mismo)
JWT_SECRET=tu_jwt_secret_muy_seguro

# Entorno
NODE_ENV=development

# Configuración de Aternity API (mantener igual)
ATERNITY_BASE_URL=https://us3-odata.aternity.com/aternity.odata/latest
ATERNITY_USER=SGLozua@teco.com.ar

# ============================================
# INSTRUCCIONES:
# ============================================
# 1. Conectate a la VPN
# 2. Completa los datos DB_HOST, DB_USER y DB_PASSWORD
# 3. Verifica el DB_PORT (normalmente es 1433 para SQL Server remoto)
# 4. Ejecuta: npm run switch:remote
# 5. Para volver a local: npm run switch:local
